# 使用 SQLite 保存结构化项目状态

汉化项目的稳定身份、文本出现位置、译文及其版本、检查状态、恢复点和任务记录统一保存在本地 SQLite 数据库中；可读配置继续使用配置文件，凭据交给操作系统凭据库，词汇和项目资产仍支持开放格式导出。相比继续维护多个整份读写的 JSON 文件，这会引入数据库迁移与访问层，但能提供事务、精确更新、历史记录和并发安全，并消除当前同名目录串项目及最后写入者覆盖的问题。
