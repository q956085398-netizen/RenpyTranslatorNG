# 使用稳定项目身份并验证式迁移游戏版本

汉化项目使用独立于目录名称和绝对路径的稳定身份，以避免不同位置的同名游戏共享项目资产。用户确认新的游戏安装属于现有项目后，词汇表、人物关系、关系词和项目设置可以直接继承；提取结果与任务清单必须重新生成，译文只有在源文本及出现位置仍然匹配时才能自动继承，其他旧译文保留为迁移建议。这样会增加迁移判断和交互成本，但可以同时保护付费翻译成果，并避免把旧版本译文静默写入错误节点。
