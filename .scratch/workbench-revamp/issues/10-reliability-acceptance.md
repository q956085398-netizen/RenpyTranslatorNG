# 10: 可靠性验收与回归全量接入

**What to build:** 把合成回归游戏接入项目身份、迁移、项目数据库、任务协调器、事务式应用、外部变更检测和编辑页的全链路,运行规格成功指标核对并记录阶段 1 验收结论。移除旧的 JSON 直读写路径(收缩步骤),使项目数据库成为唯一可信来源。本票是阶段 2(UI 重组)的启动门。

**Blocked by:** 04(旧数据自动迁移)、08(外部 tl 变更检测)、09(译文编辑页接入项目库)

**Status:** ready-for-agent

- [ ] 端到端回归覆盖:同名隔离、逐位置编辑、人工译文保护、并发不丢数据、统一应用刷新全部层、失败恢复、外部变更不静默覆盖
- [ ] 成功指标核对:串项目 0、自动覆盖人工译文 0、半应用 0、未提示覆盖外部变更 0、迁移对账率 100%
- [ ] 旧 JSON 直读写路径移除,历史数据只经迁移进入新存储
- [ ] 干净环境一条命令运行全部回归
- [ ] 验收结论与遗留事项记录在案,阶段 2 的重新切片以此为输入
